{
  "name": "@vault-agent/cli",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "vault-agent": "./dist/main.js"
  },
  "scripts": {
    "build": "tsup",
    "postbuild": "chmod +x dist/main.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@vault-agent/core": "*",
    "@vault-agent/server": "*",
    "commander": "^13.1.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "tsup": "^8.4.0",
    "typescript": "^5.8.0"
  }
}
